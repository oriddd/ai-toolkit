# Package skeleton
Copy these directories into `src/main/java/{{basePackagePath}}/` to seed
a new service with the canonical package layout described in this
skill. Each directory contains a `.gitkeep` so the empty package
survives version control.
